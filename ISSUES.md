# 課題（Issues）一覧

現在、リポジトリおよびコード内から確認された課題、および今後対応予定のタスク一覧です。

## 1. プロジェクト要件（新規開発）
- **[feat#01] ユーザー権限（管理者・一般）の分離と管理画面へのアクセス機能の実装**
    - 会員と管理者の権限を明確に分離し、適切なアクセス制限（フィルタリング）と画面遷移を実装する。

## 2. ソースコード内のTODO・改修予定
- **トップ画面の表示ロジック改修** (`ClientItemShowController.java`)
    - 「現在は全件表示を行っているが、これを売れ筋（注文回数が多い順）に改修する」というTODOの完全実装。
- **フィルター制限の有効化確認** (`FilterConfig.java`)
    - 開発用アクセス制限無効化設定の解除と、本番用制限の適用。

## 3. 実装上の不備・改善点（コード分析による）
- **ItemRepository における引数の無視**
    - `findPopularItems` 等で、引数 `deleteFlag` を受け取っていながら、クエリ内で `0` がハードコーディングされている。
- **メソッド命名と実装の不一致** (`ItemRepository.java`)
    - `Containing`（曖昧検索）という命名と、完全一致（`=`）の実装の乖離。
- **管理者用注文一覧のソート順保証** (`AdminOrderShowController.java`)
    - 注文日降順での取得を確実に保証するリポジトリ呼び出しへの変更。

## 4. ドキュメントの不整合
- **READMEのバージョン表記**
    - 「Spring Boot 4」と記載されている箇所の正確性確認（現行は4.0.6相当の設定）。
